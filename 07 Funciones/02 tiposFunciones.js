// Función incorporada: console.log()
console.log("Hola, mundo!");

// Función definida: sumar()
function sumar(a, b) {
  return a + b;
}

// Llamada a la función definida
let resultado = sumar(5, 3);
console.log(resultado);
