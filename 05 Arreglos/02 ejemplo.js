//haz una lista de arreglos
let lista = ['sony', 'samsung', 'apple', 'huawei', 'xiaomi'];

let contador = 0;
while (contador >= 0 && contador <= lista.length){
    console.log(lista[contador]);
    contador++;
}